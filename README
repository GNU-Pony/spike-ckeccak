Spike extension with C implementation of sha3sum calculation
for faster verification of downloaded sources.

This implementation is estimated to be c:a 297 times faster
then Spike's built-in Python 3 implementation.

